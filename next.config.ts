import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['pseudoexperimental-tiana-thysanurous.ngrok-free.dev'],
};

export default nextConfig;
